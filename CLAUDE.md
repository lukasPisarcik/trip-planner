# Trip Planner

Personal trip planner built with SvelteKit + Bun + Tailwind 4 + shadcn-svelte. Every trip renders the same layout (Hero + tab bar: Itinerary, Transport, Viral Spots, Flights, Budget, Tips) — see `src/lib/components/trip/` for the reusable components and `src/lib/trips/` for trip data.

## Stack

- **Runtime:** Bun (`bun install`, `bun run <script>`, `bun add`, `bunx`). Prefer Bun APIs (`Bun.env`, `Bun.file`) over Node equivalents.
- **Framework:** SvelteKit 2 with Svelte 5 runes (`$state`, `$derived`, `$effect`).
- **Validation:** Zod (one schema file at `src/lib/schemas/schemas.ts`).
- **Logging:** Pino via `src/lib/helpers/logger.ts`.
- **Styling:** Tailwind 4 + shadcn-svelte primitives in `src/lib/components/ui/`.

## Hard rules (read before editing)

1. **All Zod schemas live in `src/lib/schemas/schemas.ts`.** Never define schemas inline or in feature folders. See `.claude/docs/schemas.md`.
2. **All env vars are declared in `src/lib/server/env.server.ts` (Zod-validated).** Never read `Bun.env` / `process.env` / `$env/*` directly elsewhere. See `.claude/docs/environment-variables.md`.
3. **Never edit files in `src/lib/components/ui/`.** Those are generated by shadcn-svelte; modify them via the CLI or by overriding in your own components.
4. **Server logic goes in `src/lib/server/services/`**, called from remote functions. See `.claude/docs/server-services.md`.
5. **Tests:** `*.test.ts` for server (Bun runtime, plain TS), `*.svelte.test.ts` for client (Vitest browser project, Svelte components). See `.claude/docs/testing.md`.

## Conventions reference

Topic-by-topic deep dives live in `.claude/docs/`. Read the relevant one before making changes in that area:

- `.claude/docs/project-overview.md` — architecture diagram and folder map
- `.claude/docs/environment-variables.md` — adding env vars correctly
- `.claude/docs/api-endpoints.md` — SvelteKit endpoints and response codes
- `.claude/docs/schemas.md` — Zod patterns, shared types
- `.claude/docs/server-services.md` — service layer patterns
- `.claude/docs/testing.md` — three-runner test setup, file naming
- `.claude/docs/imports-and-exports.md` — `$lib` alias, barrel files, import order
- `.claude/docs/svelte-stores.md` — Svelte 5 class-based stores with runes
- `.claude/docs/zod-validation.md` — schema definition, `safeParse` pattern
- `.claude/docs/remote-functions.md` — thin wrappers around services
- `.claude/docs/logging-and-errors.md` — structured logging + error handling
- `.claude/docs/changesets.md` — versioning workflow

## Common commands

| Command                | Purpose                                   |
| ---------------------- | ----------------------------------------- |
| `bun run dev`          | Start dev server on http://localhost:5173 |
| `bun run check`        | TypeScript + Svelte type check            |
| `bun run lint`         | Prettier + ESLint                         |
| `bun run test:unit`    | Vitest (server + client projects)         |
| `bun run cypress:open` | Cypress E2E                               |
| `bun run build`        | Production build                          |
| `bun changeset`        | Add a changeset for your change           |

## Persistence

Trips, folders, and chat threads live in **Convex** (a hosted reactive database).
One Convex deployment is shared by local dev and any deployment; data is no longer
committed to git. See `README.md` for first-time setup (provisioning your own deployment).

- **Schema + functions** live in `src/convex/` (`schema.ts`, `trips.ts`, `folders.ts`,
  `chats.ts`). The nested trip/chat bodies are stored as `v.any()` and validated with the
  Zod schemas in `src/lib/schemas/schemas.ts` (still the single source of truth) inside the
  handlers. `src/convex/_generated/` is committed (regenerated by `bunx convex dev`).
- **Writes** are mutations gated by a shared `OWNER_WRITE_SECRET` (`src/convex/lib/secret.ts`).
  They flow through SvelteKit `command`s → the service layer (`src/lib/server/services/`) →
  a server-side `ConvexHttpClient` (`src/lib/server/data/convex.ts`) that attaches the secret.
  Never call Convex from feature code directly — go through the service layer.
- **Reads:** trips & folders are read reactively in the browser via `convex-svelte`
  `useQuery` (public queries). Chats are private AI conversations, so their queries are
  secret-gated and read only server-side through the service layer.
- **Read-only mode:** `VIEWER_MODE=true` (e.g. on Vercel) blocks writes and hides the
  co-pilot; reads still come live from Convex. The public deployment is not given
  `OWNER_WRITE_SECRET`, so it cannot write even at the backend.
